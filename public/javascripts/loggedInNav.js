window.addEventListener("load", (event) => {
    // document.querySelectorAll(".nav-link-wrapper")[0].setAttribute("hidden", "true")
    // document.querySelectorAll(".nav-link-wrapper")[1].setAttribute("hidden", "true")
    // document.querySelectorAll(".nav-link-wrapper")[2].setAttribute("hidden", "false")
    //make dynamic
  })
