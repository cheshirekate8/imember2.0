window.addEventListener("load", (event) => {
    // document.querySelectorAll(".nav-link-wrapper")[0].setAttribute("hidden", "false")
    // document.querySelectorAll(".nav-link-wrapper")[1].setAttribute("hidden", "false")
    // document.querySelectorAll(".nav-link-wrapper")[2].setAttribute("hidden", "true")
  })
